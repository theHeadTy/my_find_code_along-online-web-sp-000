require_relative "../lib/my_find.rb"

collection = (1..100).to_a

def my_find(array)
  array.select { |val|
    break val if yield(val)
  }
end

my_find(1..100) { |i| i % 3 == 0 && i % 5 == 0 }

my_find(1..100) { |i| i % 200 == 0 }